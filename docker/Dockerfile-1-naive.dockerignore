# Deliberately naive baseline.
#
# A Dockerfile-specific ignore file takes precedence over the root
# .dockerignore. Only observer artifacts generated by the benchmark harness are
# excluded; project build output, Git metadata and IDE files still enter the
# deliberately naive context.

target/benchmark/
docs/RESULTS*.md
